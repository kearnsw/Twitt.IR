angular.module('twittIR', ['ngAnimate', 'ngMaterial', 'ngMessages', 'app.routes', 'dashboard', 'dateSlider', 'activeDateService'])

// application configuration to integrate token into requests
.config(function($httpProvider) {
	
});